import FormComponent from "@/components/Form";

const page = () => {
  return (
    <main className="p-2 md:p-4 flex flex-col w-full">
      <FormComponent />
    </main>
  );
};

export default page;
